# -*- coding: utf-8 -*-
"""EMG Gesture ML Pipeline RUN ALL.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1z5xv6yt9Ytk3CplFsTmqxGMmw1Jixj_Q
"""

import os
import subprocess
import sys

def run(cmd):
  print(f"\n>>> Running: {cmd}")
  result = subprocess.run(cmd, shell=True)
  if result.returncode != 0:
      sys.exit(f"Command failed: {cmd}")

# Ensure folders exist
os.makedirs("models", exist_ok=True)
os.makedirs("results", exist_ok=True)

# 1. Train models (creates ONNX & weights)
run("python emg_gesture_ml_predictor.py")

# 2. ONNX Helpers
run("python emg_gesture_build_onnx_from_weights.py")
run("python emg_gesture_build_onnx_mlp.py")

# 3. Quantize (creates INT8 models)
run("python emg_gesture_prediction_quantization.py")

# 4. Benchmark (CPU & GPU)
run("python emg_gesture_prediction_benchmark.py")

# 5. Results plots
run("python emg_gesture_prediction_benchmark_results.py")

print("\nFull pipeline completed successfully!")